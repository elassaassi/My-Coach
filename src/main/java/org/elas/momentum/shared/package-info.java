/**
 * Module partagé — types transversaux accessibles par tous les modules.
 * Déclaré OPEN pour Spring Modulith : ses types internes sont exposés à tous.
 */
@ApplicationModule(type = ApplicationModule.Type.OPEN)
package org.elas.momentum.shared;

import org.springframework.modulith.ApplicationModule;
